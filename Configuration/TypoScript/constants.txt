
plugin.tx_t3monitoring_t3monitor {
  view {
    # cat=plugin.tx_t3monitoring_t3monitor/file; type=string; label=Path to template root (FE)
    templateRootPath =
    # cat=plugin.tx_t3monitoring_t3monitor/file; type=string; label=Path to template partials (FE)
    partialRootPath =
    # cat=plugin.tx_t3monitoring_t3monitor/file; type=string; label=Path to template layouts (FE)
    layoutRootPath =
  }
  persistence {
    # cat=plugin.tx_t3monitoring_t3monitor//a; type=string; label=Default storage PID
    storagePid =
  }
}

module.tx_t3monitoring_t3monitor {
  view {
    # cat=module.tx_t3monitoring_t3monitor/file; type=string; label=Path to template root (BE)
    templateRootPath =
    # cat=module.tx_t3monitoring_t3monitor/file; type=string; label=Path to template partials (BE)
    partialRootPath =
    # cat=module.tx_t3monitoring_t3monitor/file; type=string; label=Path to template layouts (BE)
    layoutRootPath =
  }
  persistence {
    # cat=module.tx_t3monitoring_t3monitor//a; type=string; label=Default storage PID
    storagePid =
  }
}
